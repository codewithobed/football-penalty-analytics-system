# Refactored Page Structure

This package refactors the single-file Streamlit app into page modules under `pages_app/`.

Replace your current `app.py` and `pages_app/` folder with the files from this package, then run:

```powershell
streamlit run app.py
```
